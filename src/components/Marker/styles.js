import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  priceContainer: {
    padding: 5,
    borderRadius: 20,
    borderColor: 'grey',
    borderWidth: 1,
  },
  price: {
    fontWeight: 'bold',
  },
});

export default styles;
